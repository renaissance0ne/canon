[2026-08-05T05:14:40.600Z] RESULT
============================================================
{
  "success": true,
  "output": "```json\n{\n  \"vulnerabilities\": [\n    {\n      \"severity\": \"medium\",\n      \"file\": \"web/lib/db/index.ts\",\n      \"line\": 28,\n      \"description\": \"Database connection uses `prepare: false` to work around Supabase transaction pooler limitations. While documented and a deliberate choice, this disables prepared statement protection. Ensure all queries use Drizzle ORM parameterized queries — do not construct raw SQL strings.\"\n    },\n    {\n      \"severity\": \"low\",\n      \"file\": \"web/drizzle.config.ts\",\n      \"line\": 12,\n      \"description\": \"Migration configuration accepts DATABASE_URL as fallback but prefers MIGRATION_DATABASE_URL. Ensure both variables are properly scoped to development/deployment environments and never exposed in client bundles or version control.\"\n    },\n    {\n      \"severity\": \"low\",\n      \"file\": \"web/lib/db/schema.ts\",\n      \"line\": \"~36\",\n      \"description\": \"Schema config field stores non-secret connection shape. Verify at runtime that no credentials (passwords, tokens, API keys) ever reach the `config` JSONB field. The architecture correctly documents this, but enforcement should be validated in route handlers.\"\n    }\n  ],\n  \"riskScore\": 22,\n  \"recommendations\": [\n    \"✓ **No hardcoded credentials found** — the codebase correctly uses environment variables and explicitly documents that credentials never reach the database schema or client bundles.\",\n    \"✓ **Server-only protection is correct** — `server-only` marker prevents DATABASE_URL from leaking to the browser; all secret-handling follows AGENTS.md rules.\",\n    \"✓ **ORM usage prevents SQL injection** — Drizzle ORM is used consistently; no raw SQL construction observed.\",\n    \"**Verify route handlers** — The provided context omits `/api/` endpoints. Ensure all POST/PATCH handlers validate request bodies with Zod before touching the database (AGENTS.md mandates this).\",\n    \"**Audit the engine layer** — The reconciliation engine (Python layer) must never write to `sources` or `rulesets` tables; only `entities`, `matches`, `conflicts`, and `resolutions`. Verify table-ownership constraints in `engine/db.py`.\",\n    \"**Clerk secrets** — Confirm that `CLERK_SECRET_KEY` (if used) lives only in `engine/.env` and never in `web/` environment variables. The visible templates correctly use `VITE_CLERK_PUBLISHABLE_KEY`, which is safe to expose.\",\n    \"**Validate environment on startup** — Add runtime checks in both `web/` and `engine/` to confirm required environment variables are set before the application starts (e.g., DATABASE_URL, ANTHROPIC_API_KEY for engine).\",\n    \"**Audit logging in engine** — Ensure no credentials, entity field values, or resolution rationales are logged to stdout/stderr where they might reach a log aggregator. The audit_log table should record actions, not raw data.\",\n    \"**Review Postgres pooler configuration** — The code uses transaction mode (port 6543) for app runtime but session mode for migrations. Verify the connection pooler's IP allowlist and require SSL/TLS (`sslmode=require` in DATABASE_URL).\"\n  ]\n}\n```\n\n---\n\n## Summary\n\n**Risk Level: LOW–MEDIUM** (22/100)\n\n**What's secure:**\n- No hardcoded secrets found anywhere in the provided files.\n- Architecture correctly isolates secrets to server-side environment variables using Next.js `server-only` marker.\n- Drizzle ORM provides SQL injection protection.\n- Database schema explicitly forbids credentials in the `config` JSONB field — well-documented in AGENTS.md.\n- Clerk integration uses public keys appropriately.\n\n**What needs verification:**\n- The `/api/` route handlers (not shown) must validate all inputs with Zod before querying the database.\n- The Python engine layer must respect table ownership rules — it can only write to tables it owns.\n- Environment variables must be validated at runtime in both layers.\n- Ensure no secrets leak into logs, error messages, or the browser.\n\n**Key finding:** Your AGENTS.md document is unusually thorough on security rules (see \"Security Rules\" section). The codebase appears to follow them. Spot-check the engine's `db.py` module and all route handlers in `web/app/api/` to confirm.\n",
  "parsedOutput": {
    "vulnerabilities": [
      {
        "severity": "medium",
        "file": "web/lib/db/index.ts",
        "line": 28,
        "description": "Database connection uses `prepare: false` to work around Supabase transaction pooler limitations. While documented and a deliberate choice, this disables prepared statement protection. Ensure all queries use Drizzle ORM parameterized queries — do not construct raw SQL strings."
      },
      {
        "severity": "low",
        "file": "web/drizzle.config.ts",
        "line": 12,
        "description": "Migration configuration accepts DATABASE_URL as fallback but prefers MIGRATION_DATABASE_URL. Ensure both variables are properly scoped to development/deployment environments and never exposed in client bundles or version control."
      },
      {
        "severity": "low",
        "file": "web/lib/db/schema.ts",
        "line": "~36",
        "description": "Schema config field stores non-secret connection shape. Verify at runtime that no credentials (passwords, tokens, API keys) ever reach the `config` JSONB field. The architecture correctly documents this, but enforcement should be validated in route handlers."
      }
    ],
    "riskScore": 22,
    "recommendations": [
      "✓ **No hardcoded credentials found** — the codebase correctly uses environment variables and explicitly documents that credentials never reach the database schema or client bundles.",
      "✓ **Server-only protection is correct** — `server-only` marker prevents DATABASE_URL from leaking to the browser; all secret-handling follows AGENTS.md rules.",
      "✓ **ORM usage prevents SQL injection** — Drizzle ORM is used consistently; no raw SQL construction observed.",
      "**Verify route handlers** — The provided context omits `/api/` endpoints. Ensure all POST/PATCH handlers validate request bodies with Zod before touching the database (AGENTS.md mandates this).",
      "**Audit the engine layer** — The reconciliation engine (Python layer) must never write to `sources` or `rulesets` tables; only `entities`, `matches`, `conflicts`, and `resolutions`. Verify table-ownership constraints in `engine/db.py`.",
      "**Clerk secrets** — Confirm that `CLERK_SECRET_KEY` (if used) lives only in `engine/.env` and never in `web/` environment variables. The visible templates correctly use `VITE_CLERK_PUBLISHABLE_KEY`, which is safe to expose.",
      "**Validate environment on startup** — Add runtime checks in both `web/` and `engine/` to confirm required environment variables are set before the application starts (e.g., DATABASE_URL, ANTHROPIC_API_KEY for engine).",
      "**Audit logging in engine** — Ensure no credentials, entity field values, or resolution rationales are logged to stdout/stderr where they might reach a log aggregator. The audit_log table should record actions, not raw data.",
      "**Review Postgres pooler configuration** — The code uses transaction mode (port 6543) for app runtime but session mode for migrations. Verify the connection pooler's IP allowlist and require SSL/TLS (`sslmode=require` in DATABASE_URL)."
    ]
  },
  "durationMs": 35313,
  "model": "haiku",
  "sandboxMode": "strict",
  "workerType": "audit",
  "timestamp": "2026-08-05T05:14:40.600Z",
  "executionId": "audit_1785906845287_ijs5kr"
}
